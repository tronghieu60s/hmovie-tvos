export type MovieInfoType = {
  id: string;
  name: string;
  slug: string;
  originName: string;
  thumbUrl: string;
  posterUrl: string;
};

export type MovieListType = {
  id: string;
  name: string;
  slug: string;
  originName: string;
  thumbUrl: string;
  posterUrl: string;
};

